name: flutter_webln
description: A WebLN interface for creating Bitcoin Lightning powered web applications.
version: 0.0.1
homepage: https://github.com/aniketambore/flutter_webln

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  js: ^0.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter: